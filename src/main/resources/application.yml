spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
      url: jdbc:mysql://localhost:3306/customerapi
      username: root
      password: root

server:
  port: 8081