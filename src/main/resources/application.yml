spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
      url: jdbc:mysql://localhost:3306/customerapi
      username: root
      password: admin

server:
  port: 8081